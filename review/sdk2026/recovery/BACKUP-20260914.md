# Directly accessible recovery backup — 2026-09-14

Created at the operator's request. Supervisor backup feec49c2, name SDK2026-recovery-20260914, is retained on HA at /backup/feec49c2.tar. Archive permissions are root0600; it is NOT password-encrypted (Supervisor protected=false). Access is through the documented authenticated ha-ssh workflow. No raw backup or secrets are stored in this repository.

Size129986560 bytes (approximately124MiB). SHA2560d3d71398a963bb9199aee781b1560ba5509f2de5a563f068758077b21f5d141.

## Verified coverage

Supervisor metadata: HA2026.9.2, focused Multiprotocol0.2.3-ordered, Z2M2.14.1-1 and Matter9.2.0. Outer and nested archives were listed successfully. Explicitly confirmed:

- HA config: .storage/thread.datasets and .storage/core.config_entries.
- Z2M's ACTUAL configuration directory: zigbee2mqtt/configuration.yaml, database.db, state.json and coordinator_backup.json.
- Focused app: data/thread settings file, data/zigbeed/host_token.nvm, options and local-state/import markers.
- Matter app: chip configuration/counters, controller JSON and current server/config storage, certificates and supporting state.

The history exclusion flag was supplied without an explicit value, but inspection found home-assistant_v2.db and its WAL in the archive. Therefore this backup INCLUDES history database files; do not describe it as a database-excluded backup or assume the flag had the intended effect. Its total size is modest, so no repeat backup was needed merely to reduce size. Database consistency/restoration was not tested.

All three apps reported started after backup. Their inspected configurations use default hot backup without identified cold mode/pre/post hooks; no manual restart, radio reset or configuration change occurred. HA peer reported no conflicting component changes.

## Direct operations

Use scripts/ha-ssh after the documented ha-unlock --check-access:

    ha backups info feec49c2 --raw-json
    sha256sum /backup/feec49c2.tar
    tar -tf /backup/feec49c2.tar

Archive extraction/restore must keep secret contents private. Supported Supervisor restoration exists, but is disruptive and remains separately authorized; it was NOT tested by restoring production. This is a current host-state backup, not a complete machine backup, retained container-image backup, or physical-radio firmware/NVM backup. It closes the observed missing host-state coverage in524be610, but does not establish firmware downgrade safety or an atomic snapshot across all live services. Continue the separate radio recovery investigation.